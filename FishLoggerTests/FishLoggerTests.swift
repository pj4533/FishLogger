import Testing
@testable import FishLogger

// Placeholder top-level suite. Individual feature suites live in their own files.
struct FishLoggerTests {
    @Test func smoke() {
        #expect(true)
    }
}
